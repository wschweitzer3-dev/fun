# Doc Side Kick Databricks App

Minimal Streamlit app that queries the DocSideKick Supervisor serving endpoint.

## Expected env
- `SUPERVISOR_ENDPOINT_NAME` (configured in `app.yaml`)

## Run locally (optional)
```bash
streamlit run streamlit_app.py
```
